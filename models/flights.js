const https = require('https');
const querystring = require('querystring');

class Flight {
    constructor(apiUrl, apiKey) {
        this.apiUrl = apiUrl;
        this.apiKey = apiKey;
    }
    
    searchFlight(params){
    }
}